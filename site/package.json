{
  "name": "ghost-rivers-site",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "Front end for Ghost Rivers of Toronto — MapLibre GL + PMTiles, static, no vendor tokens.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "^6.0.3",
    "vite": "^8.1.3"
  },
  "dependencies": {
    "maplibre-gl": "^5.24.0",
    "pmtiles": "^4.4.1"
  }
}
